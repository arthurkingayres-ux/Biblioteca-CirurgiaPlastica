# Code Review Board — feature/phase-7-6b-3-palpebra-images  (2026-04-27)

**Title:** Phase 7.6b.3 — Palpebra Images (harvester + 19 PNGs + cards wiring + SW bump v30→v31)
**Base..Head:** master..HEAD (4 commits initial; 5th and 6th added during auto-fix)
**Files changed:** 27 (+ test + harvester refactor)
**Total findings:** 0+1+4=5 (all auto-applied)

## Blockers

_No blockers._

## Important

1. **PDF_ROOT default is fragile to worktree depth assumption** — score 41
   `tools/harvest_palpebra_images.py:52` · correctness · general-swe-reviewer
   Reviewer flagged `REPO_ROOT.parent.parent / "00-Livros-Texto"` ternário como dead code under specific assumption. After re-analysis the ternary was actually correct under the project's worktree pattern, but the brittleness to repo-folder rename was real. Replaced with `_resolve_pdf_root(repo_root)` helper that tries `repo_root/00-Livros-Texto/` first then `repo_root.parent.parent/...` as worktree fallback — robust to any directory naming.

## Suggestions

2. **Docstring path mismatch with OUTPUT_DIR** — score 27
   `tools/harvest_palpebra_images.py:7` · correctness · general-swe-reviewer
   Docstring referenciava `assets/images/reconstrucao-facial/reconstrucao-de-palpebra/` mas OUTPUT_DIR usa `assets/images/reconstrucao-de-palpebra/` (path foi achatado). Corrigido.

3. **`__doc__.split` quebra sob `python -OO`** — score 30
   `tools/harvest_palpebra_images.py:141` · correctness · general-swe-reviewer
   `(__doc__ or "").split("\n")[0]` defensivo aplicado.

4. **PDF reaberto a cada figura — Oculofacial aberto 14 vezes** — score 28
   `tools/harvest_palpebra_images.py:117` · performance · general-swe-reviewer
   `_render` agora aceita um `fitz.Document` aberto; `main()` mantém `docs_cache: dict[Path, Document]` e fecha tudo no `finally`. Cada PDF abre uma única vez por execução.

5. **Sem teste para `_ensure_ascii`** — score 18
   `tools/harvest_palpebra_images.py:106` · test-coverage · general-swe-reviewer
   Adicionado `tools/tests/test_harvest_palpebra_images.py` com 4 testes (4/4 PASSED): aceita slug ASCII puro; rejeita diacrítico; rejeita non-ASCII genérico; valida que MAPPING tem 19 entradas únicas todas ASCII e `.png`.

## Reviewers sem achados

- rag-integrity-reviewer: scope matched 0 files · 0 findings (no RAG changes in this branch)
- cards-schema-reviewer: scope matched 2 files (anatomia.json + tecnicas.json) · 0 findings (AJV 585/585; image refs all resolve; ID prefix consistent)
- pwa-frontend-reviewer: scope matched 1 file (sw.js) · 0 findings (CACHE_NAME bumped v30 → v31 correctly per project precedent)
- image-assets-reviewer: scope matched 19 PNGs + 2 cards · 0 findings (ASCII names verified, no PNG+JPG pairs, no reuse, anatomy v2 has no images per Phase 7.2 rule)
- docs-memory-reviewer: scope matched 0 files · 0 findings (theme already registered in 7.6b.2)

## Auto-aplicação (CLAUDE.md §10 step 4)

Todos os 5 findings auto-aplicados em commit subsequente. Re-validação pós-fix:
- pytest test_harvest_palpebra_images: 4/4 PASSED
- AJV strict: 585/585 OK, FAIL 0
- validate_briefings: ALL PASS
- audit_images: 0 quebradas

## Meta

Generated by `code-review-board` skill · 2026-04-27 09:30 BRT · 6 reviewers
- cards-schema redispatched once after initial rejection
- image-assets redispatched twice after rejections
