# Code Review Board — PR #28  (2026-04-20)

**Title:** Phase 7.4: Reconstrucao de Nariz (Wave 1 sub-unit 1)
**Base..Head:** master..feature/phase-7-4-reconstrucao-nariz
**Files changed:** 61
**Total findings:** 1 blocker + 2 important + 3 suggestions = 6

> Calibration run — produced by the `code-review-board` skill on its inaugural dry-run against an already-merged PR. Committed as an artifact to prove the pipeline works end-to-end.

## Blockers

1. **SW asset bump missing — `webapp/library/` assets changed but `sw.js` `CACHE_NAME` still at v27** — score 76
   `webapp/library/sw.js:1` · sw-version-not-bumped · pwa-frontend-reviewer
   PR adds 15 PNGs under `webapp/library/assets/images/reconstrucao-de-nariz/` and modifies `webapp/library/rag-index.json`, but `sw.js` is not in the diff. Runtime impact is limited (new PNGs flow through the runtime `/assets/` cache handler; `rag-index.json` is not precached), but the project invariant "bump SW when any `webapp/library/` asset changes" is violated. Previous phases bumped (Phase 7.0 v25 → 7.1 v26 → 7.2 v27). Phase 7.3 and 7.4 silently skipped. Recommend bumping to `briefing-preop-v28` in a follow-up.

## Important

1. **`tools/build_rag_index.js` ships 100% CRLF line endings** — score 52
   `tools/build_rag_index.js:1` · crlf-or-encoding · cards-schema-reviewer
   The parser uses `split(/\r?\n/)`, so input handling is safe, but the script file itself being CRLF on a Unix-oriented Node pipeline is the same failure class that motivated the BM25 69→734 chunk fix noted in the PR. A `.gitattributes` rule pinning EOL for JS/MD would prevent recurrence. Not runtime-blocking.
2. **All 6 new card JSONs committed as CRLF** — score 48
   `content/cards/reconstrucao-facial/reconstrucao-de-nariz/_meta.json:1` · crlf-or-encoding · cards-schema-reviewer
   `_meta.json`, `anatomia.json`, `decisoes.json`, `flashcards.json`, `notas.json`, `tecnicas.json` all read as CRLF. `JSON.parse` is EOL-agnostic so no runtime impact, but the CRLF surface area is exactly what bit the BM25 index in the past. Apply `.gitattributes` for `content/cards/**/*.json` to normalize to LF going forward.

## Suggestions

1. **Per-file image JSONs duplicate `content/images/manifest.json` — drift risk** — score 21
   `content/images/reconstrucao-de-nariz/nariz-bilobed-ponta-aplicacao.json:2297` · correctness · general-swe-reviewer
   All 15 new per-file descriptors carry the same id/file/subject/role/source/credit/default_caption/labels/applicable_topics/status fields also present in `content/images/manifest.json`. Without a regenerate-on-write script checked in, editing one and forgetting the other will silently drift. Recommend either (a) per-file JSON is source of truth with a build step to rewrite the manifest, or (b) drop the per-file JSONs. Flagged once at the first file; applies to all 15.
2. **Umbrella spec `2026-04-18-reconstrucao-facial-subunidade-design.md` has no matching plan file** — score 21
   `docs/superpowers/specs/2026-04-18-reconstrucao-facial-subunidade-design.md:1` · spec-plan-drift · docs-memory-reviewer
   Phase 7.4 has its own narrow spec + plan pair so execution isn't blocked, but the umbrella spec covering the whole sub-unidade wave (nose, lip, ...) is orphaned. Convert to an index/README or create a short umbrella plan listing the sub-unit PRs as a roadmap.
3. **`content/images/manifest.json` lacks trailing newline** — score 19
   `content/images/manifest.json:2287` · correctness · general-swe-reviewer
   Regenerated manifest ends without a final newline (`\ No newline at end of file` in the patch). Minor POSIX convention issue — breaks `cat`/`wc -l` assumptions. Emit `JSON.stringify(...) + "\n"` in the generator.

## Reviewers sem achados

- rag-integrity-reviewer: scope hit 1 file (`content/rag/reconstrucao-facial/reconstrucao-de-nariz.md`) · 0 findings — content entirely additive, citations pervasive (Kaufman 2018, Neligan 2023), textbook anchors throughout, acronyms expanded on first use.
- image-assets-reviewer: scope hit 15 files under `assets/images/reconstrucao-de-nariz/` · 0 findings — all ASCII, no duplicate extensions, no cross-card reuse, cards are text-only (no `images[]`) so overlay mandate N/A.

## Meta

Generated by `code-review-board` skill · 2026-04-20 (calibration dry-run) · 6 reviewers dispatched in parallel via general-purpose subagent with embedded system prompts.

### Calibration verdicts

- **Dispatch mechanism:** ✅ all 6 returned valid `### FINDINGS ... ### END` blocks.
- **Hallucination guard:** ✅ both non-diff paths (`webapp/library/sw.js`, spec file) validated as existing on disk.
- **Known-regression catch:** ✅ cards-schema caught the CRLF pattern — the same class that produced MEMORY entry "fix CRLF BM25 69→734 chunks".
- **Out-of-scope restraint:** ✅ image-assets returned empty when cards had no `images[]` fields; rag-integrity returned empty when content was additive and well-cited.
- **Anti-duplication (general-SWE):** ✅ suppressed findings for `tools/build_rag_index.js` (owned by cards-schema) and image manifest labels (owned by image-assets), emitted only on `content/images/**` (genuinely unowned area).
- **Prompt error surfaced:** ⚠️ the `pwa-frontend-reviewer` agent prompt referred to `webapp/library/service-worker.js`, but the real file is `webapp/library/sw.js`. The agent inferred the right file anyway and flagged correctly, but the SKILL.md + agent prompt should be patched to `sw.js`. Logged as follow-up.

### Planned validation deltas

- Plan expected `pwa-frontend-reviewer` to return empty (out of scope). Plan was wrong — PR touches `webapp/library/rag-index.json` and PNG assets under `webapp/library/`. The reviewer correctly flagged the SW version drift; this is a valid finding, not a scope violation.
